/**
 * @deprecated This file has been split into single-responsibility modules.
 * Import from the specific files instead:
 *
 *   location.ts       — ghl_get_location
 *   tags.ts           — ghl_*_location_tag
 *   custom_fields.ts  — ghl_*_custom_field
 *   custom_values.ts  — ghl_*_custom_value
 *   users.ts          — ghl_*_user
 *   templates.ts      — ghl_*_template
 *   forms.ts          — ghl_get_forms, ghl_get_form_submissions
 *
 * This file is intentionally empty and will be removed in a future cleanup.
 */

export const locationTools = [];
