/**
 * DEPRECATED — this file is a tombstone.
 *
 * Location-related tools were split into single-responsibility modules:
 *   location.ts        — ghl_get_location
 *   tags.ts            — ghl_*_location_tag
 *   custom_fields.ts   — ghl_*_custom_field
 *   custom_values.ts   — ghl_*_custom_value
 *   users.ts           — ghl_*_user
 *   templates.ts       — ghl_*_template
 *   forms.ts           — ghl_get_forms, ghl_get_form_submissions
 *   surveys.ts         — ghl_get_surveys, ghl_get_survey_submissions
 *
 * Import directly from those modules. Do NOT add real exports here.
 */

export const locationTools: never[] = [];
